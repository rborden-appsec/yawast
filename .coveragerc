[run]
omit =
    # omit anything in a .local directory anywhere
    */.local/*
    # omit everything in /usr
    /usr/*
    # omit site-packages & other outside files
    */site-packages/*
    */virtualenv/*
